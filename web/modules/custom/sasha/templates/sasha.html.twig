{#
/**
 * @file
 * Default theme implementation to present a sasha entity.
 *
 * This template is used when viewing a registered sasha's page,
 * e.g., /admin/content/sasha)/123. 123 being the sasha's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_sasha()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
